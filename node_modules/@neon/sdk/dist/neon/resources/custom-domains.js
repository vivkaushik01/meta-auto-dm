import { deleteProjectBranchCustomDomain, listProjectBranchCustomDomains, registerProjectBranchCustomDomain } from "../../client/sdk.gen.js";
import { paginate } from "../paginate.js";
//#region src/neon/resources/custom-domains.ts
/** Branch-scoped custom domains. v1 can only target a function. */
var CustomDomains = class {
	#ctx;
	constructor(ctx) {
		this.#ctx = ctx;
	}
	/** @apiCall GET /projects/{project_id}/branches/{branch_id}/custom-domains (cursor-paginated) */
	list(projectId, branchId, query, opts) {
		return paginate((cursor, signal) => listProjectBranchCustomDomains({
			client: this.#ctx.client,
			path: {
				project_id: projectId,
				branch_id: branchId
			},
			query: {
				...query,
				cursor
			},
			throwOnError: false,
			signal
		}), (data) => ({
			items: data?.custom_domains ?? [],
			cursor: data?.pagination?.next
		}), () => this.#ctx.deadlineFor(opts));
	}
	register(projectId, branchId, input, opts) {
		return this.#ctx.run(opts, (client, signal) => registerProjectBranchCustomDomain({
			client,
			path: {
				project_id: projectId,
				branch_id: branchId
			},
			body: input,
			throwOnError: false,
			signal
		}), (data) => data);
	}
	delete(projectId, branchId, domain, opts) {
		return this.#ctx.runVoid(opts, (client, signal) => deleteProjectBranchCustomDomain({
			client,
			path: {
				project_id: projectId,
				branch_id: branchId,
				domain
			},
			throwOnError: false,
			signal
		}));
	}
};
//#endregion
export { CustomDomains };

//# sourceMappingURL=custom-domains.js.map